import { types } from "./types.js";
import { mutations } from "./mutation.js";
import { resolver } from "./resolver.js";
import { queries } from "./query.js";

export const Tweet = { types, mutations, resolver, queries };
